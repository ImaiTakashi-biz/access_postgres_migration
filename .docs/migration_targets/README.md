# 移行対象フォルダ

Access DBごとの解析資料、設定ファイル、移行対応表をここに配置します。

例:

```text
.docs/migration_targets/
└─ sample_target/
   ├─ .env
   ├─ source.accdb
   ├─ migration_mapping.md
   └─ notes.md
```

`.env` とAccess DBファイルはGit管理しません。
